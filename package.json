{
  "name": "gs-social-scheduler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "node -e \"const { spawnSync } = require('node:child_process'); const run = (cmd, args) => { const result = spawnSync(cmd, args, { stdio: 'inherit', shell: true, env: process.env }); if (result.status) process.exit(result.status); }; run('npm', ['run', 'build']); run('wrangler', ['deploy', '--config', 'src/frontend/dist/gs_social_scheduler/wrangler.json']); run('wrangler', ['pages', 'deploy', 'src/frontend/dist/client', '--project-name', 'gs-social-scheduler', '--branch', 'main']);\"",
    "types": "wrangler types",
    "db:migrate:production": "wrangler d1 migrations apply pl8ypus-scheduler-db-prod --remote"
  },
  "dependencies": {
    "hono": "^4.12.31",
    "jose": "^6.2.3",
    "react-router-dom": "^7.18.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.45.1",
    "@cloudflare/vitest-pool-workers": "^0.8.55",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "~3.2.4",
    "wrangler": "^4.112.0"
  }
}
