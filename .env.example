# Production secret names. Set values with `wrangler secret put <NAME>`.
# Alternatively, save LinkedIn credentials from the Admin page (stored encrypted in D1).

# LINKEDIN_CLIENT_ID=
# LINKEDIN_CLIENT_SECRET=
# Optional override for encrypting Admin-saved credentials (defaults to CF_ACCESS_AUD):
# CREDENTIALS_ENCRYPTION_KEY=
