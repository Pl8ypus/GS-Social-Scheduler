# Environment template — copy to `.dev.vars` for local dev.
# Production/staging secrets use `wrangler secret put --env <env>`.

# LINKEDIN_CLIENT_ID=
# LINKEDIN_CLIENT_SECRET=
# LINKEDIN_ACCESS_TOKEN=
